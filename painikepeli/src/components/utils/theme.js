import { createMuiTheme } from "@material-ui/core";

export const theme = createMuiTheme({
    palette: {
        primary: {
           main: '#757575',
            basic: '#e8eaf6',
            light: 'white'
        },
        secondary: {
            main: "#1a237e",
            dark: '#757575'
        },
    },
});
